import random

def task(theme):
    task.counter += 1
    print(f"\n--- ЗАДАНИЕ №{task.counter}. {theme} ---")
task.counter = 0
"""
# 1. Условные операторы (10 заданий)
task("Чётное или нечётное") # Введите число, выведите, чётное оно или нет.
num1 = int(input("Введите целое число #1: "))
if num1 % 2 == 0:
    print(f"Число {num1} четное")
else:
    print(f"Число {num1} нечетное")

task("Положительное, отрицательное или ноль") # Определите знак введённого числа.
num2 = int(input("Введите целое число #2: "))
if num2 > 0:
    print(f"Число {num2} положительное")
elif num2 < 0:
    print(f"Число {num2} отрицательное")
else:
    print(f"Вы ввели {num2}")

task("Минимум из двух") # Введите два числа, выведите меньшее из них.
if num1 < num2:
    print(f"Наименьшее число - #1: {num1}")
elif num1 > num2:
    print(f"Наименьшее число - #2: {num2}")
else:
    print("Числа равны!")

task("Максимум из трёх") # Введите три числа, найдите наибольшее.
num3 = int(input("Введите целое число #3: "))
if num1 > num2 and num1 > num3:
    print(f"Наибольшее число - #1: {num1}")
elif num2 > num1 and num2 > num3:
    print(f"Наибольшее число - #2: {num2}")
elif num3 > num1 and num3 > num2:
    print(f"Наибольшее число - #3: {num3}")
else:
    num_max = max(num1, num2, num3) # sorted([a, b, c])[-1]
    print(f"Наибольшие числа равны! Максимальное значение: {num_max}")

task("Доступ по возрасту") # Введите возраст, выведите «Доступ разрешён», если от 18 и выше.
num4 = int(input("Введите возраст: "))
if num4 >= 18:
    print("Доступ в таверну разрешён")
else:
    print("Доступ молокососам запрещен")

task("Калькулятор: +, -, *, /") # Введите два числа и операцию, выполните её.
a = float(input("Первое число: "))
b = float(input("Второе число: "))
op = input("Операция (+ - * /): ")
if op == '+':
    print(f"{a} {op} {b} = {a+b}")
elif op == '-':
    print(f"{a} {op} {b} = {a-b}")
elif op == '*':
    print(f"{a} {op} {b} = {a*b}")
elif op == '/':
    if b != 0:
        print(f"{a} {op} {b} = {a/b:.2f}")
    else:
        print("Деление на ноль!? Кинь спасбросок интеллекта")
else:
    print("Что-то на инфернальном. В другой раз попробуй + - * или /")

task("Проверка логина/пароля") # Сравните введённые значения с сохранёнными.
LOGIN = "Гендальф"
PASSWORD = "ТыНеПройдешь"
login = input("Введите логин: ")
password = input("Введите пароль: ")
if login == LOGIN and password == PASSWORD:
    print("Добро пожаловать, о, великий волшебник!")
else:
    print("*Телепортирует в болото*")

task("Кратность 3 и 5") # Введите число, проверьте, делится ли оно и на 3, и на 5.
num35 = int(input("Введите целое число: "))
if num35 % 3 == 0 and num35 % 5 == 0: # можно просто кратность 15-ти
    print("Это что, пивной микс на троих? Легко разделим на пятерых!)")
else:
    print("Либо микс не на троих, либо на 5 не делится...)")

task("Проверка високосного года") # Год считается високосным, если кратен 4, но не кратен 100, или кратен 400.
num365 = int(input("Введите год: "))
if (num365 % 4 == 0 and num365 % 100 != 0) or (num365 % 400 == 0):
    print("Это високосный год! +1 день к приключениям!")
else:
    print("Год не високосный")

task("Простое число или нет") # Введите число и проверьте, делится ли оно только на 1 и само себя (if и for).
num42 = int(input("Введите целое число (для проверки): "))
eazypeazy = "- простое число"
ochen_slozhna = f"- не простое число"
if num42 <= 0:
    print(num42, ochen_slozhna)
elif 1 <= num42 <= 3:
    print(num42, eazypeazy)
else:
    status = eazypeazy #Пока не найдены делители - простое
    for i in range(2, int(num42**0.5) + 1): #Ищем делители от 2 до корня+1
        if num42 % i == 0:
            status = ochen_slozhna
            break #Если нашли делитель - вышли из цикла for, но мы все еще в else
    print(num42, status)

# 2. Цикл while (10 заданий)
task("Счёт от 1 до 10") # Используйте while, чтобы вывести числа от 1 до 10.
i = 1
while i <= 10:
    print(i)
    i += 1
#for i in range(1, 11):
#    print(i)
#    i += 1
task("Сумма чисел до 0") # Вводите числа, пока не введено 0. Суммируйте их.
sumsum = 0
i = None # Добавила эту строку по совету нейронки. None не равно нулю получается.
while i != 0:
    i = int(input("Введите целое число (0 - выход): "))
    sumsum += i
print("Сумма введенных чисел: ", sumsum)

task("Подсчёт попыток входа") # У пользователя 3 попытки ввести правильный пароль.
PAROL = "666"
p = 1
while p <= 3:
    parol = input(f"Введите пароль (попытка {p} из 3): ")
    if PAROL == parol:
        print("Пароль принят. Слава Азатоту!")
        break
    else:
        print("Пароль неверный, в гневе я. Огонь и дым летят в тебя!")
    p += 1

task("Таблица умножения на 5") # Выведите таблицу умножения от 1 до 10 (5 × 1, 5 × 2...).
digit = 5 # можно input("Введите целое число для создания таблицы: ")
k = 1
while k <= 10:
    print(f"{digit} * {k} = {int(digit) * k}")
    k += 1

task("Факториал числа") # Введите число, выведите его факториал (используя while).
numnum = int(input("Введите натуральное число (для вычисления факториала): "))
fucktorial = 1 #Если 0 (или вдруг отриц. число)
f = 1
while f <= numnum:
    fucktorial *= f
    f += 1
#for i in range(1,numnum + 1):
#    fucktorial += i
print(fucktorial)

task("Угадай число") # Компьютер загадывает число от 1 до 100, пользователь угадывает.
rand_num = random.randint(1, 100) #Тут целое число с 1 по 100 включительно
user_num = int(input("Попробуй угадать натуральное число от 1 до 100: "))
while (rand_num != user_num):
    if (rand_num > user_num):
        print("Загаданное число больше")
    else:
        print("Загаданное число меньше")
    user_num = int(input("Попробуй снова угадать или нажми 0, чтобы выйти: ")) #Добавила выход
    if (user_num == 0):
        print("Ответ:",rand_num,'\nНичего, в следующий раз угадаешь!')
        break
if (user_num != 0):
    print("Поздравляю! Ты - медиум экстрасекс!!")

task("Цикл с ошибочным вводом") # Вводите число, пока не будет введено целое число (try-except можно добавить).
while True:
    digit = input("Введите число (проверка на целостность): ")
    try:
        num = int(digit)
        print("Число целое")
        break
    except ValueError:
        print("Число не целое")

task("Чётные числа до 20") # Выведите чётные числа от 2 до 20.
i = 2
while i <= 20:
    print(i)
    i += 2

task("Ввод только положительных чисел")
# Вводите числа, пока не встретится отрицательное. После этого — сумма всех введённых положительных.
sum = 0
i = 1
while i >= 0:
    i = int(input("Введите положительное число (отрицательное - выход): "))
    sum += i
print(f"Сумма введенных положительных чисел: {sum-i}")
# Нейронка говорит тут "неочевидная логика" и надо делать if i < 0 break и только после суммировать, но мне так нравится

task("Пароль с подтверждением") # Пользователь вводит пароль до тех пор, пока не совпадут два ввода подряд.
PUPUPU = "Пуньк"
s = 0
while s < 2:
    pupupu = input("Введите пароль: ")
    if pupupu == PUPUPU:
        s += 1
    else:
        s = 0 #Счетчик обнулен
print("Пароль введен верно дважды")
"""
# 3. Цикл for (10 заданий)
task("Сумма чисел от 1 до N") # Введите число N, выведите сумму чисел от 1 до N.
N = int(input("Введите N: "))
result = 0
for elem in range(1, N + 1):
    result += elem
print(f"Сумма от 1 до {N}: {result}")

task("Вывод всех символов строки") # Введите строку, выведите каждый символ на новой строке.
stroka = input("Введите строку (только короткую): ")
for elem in stroka:
    print(elem)

task("Поиск минимального в списке") # Введите 5 чисел, найдите минимальное.
numbers = []
print("Сейчас будем вводить 5 чисел циклом for")
for i in range(5):
    num = int(input(f"Число {i+1}: "))
    numbers.append(num)
min_num = numbers[0]  # Пусть пока первое число - минимальное
for elem in numbers:
    if elem < min_num:
        min_num = elem
print("Минимальное число:", min_num)

#Ниже решение нейронки, которое я даже поняла :)
#numbers = [int(input(f"Число {i+1}: ")) for i in range(5)]
#print("Минимальное число:", min(numbers))

task("Подсчёт количества чётных чисел") # Введите список, посчитайте чётные элементы.

task("Создание новой строки без гласных") # Введите строку, уберите гласные (используйте for).

task("Вывод таблицы умножения от 1 до 10") # Используйте вложенный for.

task("Генерация списка квадратов чисел") # От 1 до 10 — создайте список квадратов.

task("Подсчёт слов в строке") # Используйте split() и for, чтобы посчитать количество слов.

task("Разделение строки на символы через запятую") # Используйте for, чтобы создать строку с запятыми между символами.

task("Нахождение среднего значения") # Введите несколько чисел, найдите среднее.

# 4. Срезы строк (10 заданий)
task("Вывод первых 5 символов строки") # Введите строку, выведите первые 5 символов.

task("Вывод последних 3 символов строки") # Используйте отрицательные индексы.

task("Вывод строки задом наперёд") # Срез с шагом -1.

task("Каждый второй символ") # Строка, срез с шагом 2: строка[::2].

task("Проверка палиндрома") # Сравните строку и её обратный вариант.

task("Удаление первых и последних символов") # строка[1:-1].

task("Разворот слов в строке") # Введите строку и переверните каждое слово по отдельности.

task("Выделение домена из email") # Из строки user@example.com выведите только example.com.

task("Подстрока между двумя символами") # Найдите строку между первым и последним пробелом.

task("Смена регистра у каждой второй буквы") # Используйте срезы и join, чтобы чередовать верхний/нижний регистр.

# 5. Комбинированные задачи (10 заданий)
task("Цифры строки") # Введите строку, используя for и if, выведите только цифры из неё.

task("Интерактивный калькулятор с циклом")
# Пока пользователь не введёт «выход», программа принимает два числа и операцию, считает результат.

task("Подсчёт слов, начинающихся с определённой буквы")
# Введите строку, разделите на слова, посчитайте, сколько из них начинаются на a.

task("Сумма всех чётных цифр в строке") # Например, abc12345 → чётные: 2 и 4 → сумма = 6.

task("Пароль-валидатор")
# Пароль должен быть не менее 8 символов, содержать цифру, заглавную и строчную буквы. Проверка с for и if.

task("Угадай слово по буквам")
# Загаданное слово скрыто (например, python → ______). Пользователь вводит буквы, пока не угадает всё.

task("Генерация новой строки без пробелов и гласных") # Введите строку, используя for и if, удалите пробелы и гласные.

task("Разделение строки на пары символов") # Например, из "abcdef" сделать ["ab", "cd", "ef"].

task("Вывод чисел от 1 до 100, кратных 3 или 5") # Используйте for, if.

task("Проверка уникальности символов в строке") # Введите строку, проверьте, все ли символы в ней уникальны (без повт.).
print('\nСпасибо за внимание!:)')